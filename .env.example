# Passcode required to log in to /admin and post poems
ADMIN_PASSCODE=Tryagain878787

# Secret used to sign session cookies — set this to a long random string in production
SESSION_SECRET=replace-with-a-long-random-string

# Optional: path to a directory for persistent SQLite storage (e.g. a Railway volume mount)
# DATA_DIR=/data
